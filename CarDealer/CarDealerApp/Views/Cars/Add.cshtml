@{
    ViewBag.Title = "Add";
}

<h2>Add Car</h2>

@using (Html.BeginForm())
{
    <div class="form-group">
        @Html.Label("Make")
        @Html.Editor("Make", new { htmlAttributes = new { @class = "form-control" } })
    </div>
    <div class="form-group">
        @Html.Label("Model")
        @Html.Editor("carModel", new { htmlAttributes = new { @class = "form-control" } })
    </div>
    <div class="form-group">
        @Html.Label("Travelled Distance")
        @Html.Editor("travelledDistance", new { htmlAttributes = new { @class = "form-control" } })

    </div>
    <div class="form-group">
        @Html.Label("Parts")
        @Html.Editor("Parts", new { htmlAttributes = new { @class = "form-control" } })
    </div>
    <div class="form-group">
        <input type="submit" value="Add" class="btn btn-default btn-lg" />
    </div>
}